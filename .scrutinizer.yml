# .scrutinizer.yml
build:
    tests:
        override:
            -
                command: phpunit --coverage-clover=my-coverage-file
                coverage:
                    file: my-coverage-file
                    format: php-clover
# before_commands:
#     - "composer install --dev"